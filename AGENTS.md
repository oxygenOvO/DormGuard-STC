# DormGuard Development Rules

## Hardware

MCU: STC15F2K60S2

System clock: 11059200 Hz

Board A: 现场感知节点

Board B: 控制与通信网关

PC: Qt/C++ 上位机

## Coding Rules

1. 必须优先使用项目中已有的 BSP API。
2. 调用任何 BSP 函数前，必须先在项目中找到它的声明或已有使用实例。
3. 严禁自行虚构不存在的 BSP API。
4. 如果无法确定某个接口，应明确告诉开发者，而不是猜测。
5. 尽量避免阻塞式 delay。
6. 遵循现有事件驱动框架。
7. 状态变量需要跨事件保存时使用合适的 global/static 变量。
8. 数码管显示应集中在周期任务中更新。
9. 不随意修改老师提供的 BSP 底层文件。
10. 不因为实现一个功能而重构无关代码。
11. 每次修改尽可能保持小而独立。
12. 修改后必须说明修改了哪些文件。
13. 必须给出实际硬件测试方法。
14. `docs/protocol.md` 是通信协议唯一依据。
15. 不得在未同步 `protocol.md` 的情况下改变通信命令值。
16. A、B、PC 三端必须使用相同协议定义。

## Responsibility

`board_A`：成员 1 负责，包括现场感知、报警、A 端通信与心跳。

`board_B`：成员 2 负责，包括本地控制、状态管理、通信网关与掉线判断。

`pc_host`：成员 3 负责，包括 Qt 上位机、串口通信、状态显示、控制和日志。

`docs`：三人共同维护。
